import {createSlice} from "@reduxjs/toolkit";



const infoCinemaSlice = createSlice({
    name: "infoCinema",
    initialState: {
        move_cinema: []
    },
    reducers: {
    }
})

export const {} = infoCinemaSlice.actions;

export default infoCinemaSlice.reducer